name: GitHell

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v2

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Abyss
        run: mkdir -p abyss

      - name: GPG Key
        uses: crazy-max/ghaction-import-gpg@v5
        with:
          gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.GPG_PASSPHRASE }}
          git_config_global: true
          git_user_signingkey: true
          git_commit_gpgsign: true

      - name: Commit and Push 100 Changes
        run: |
          git config user.name "0x3EF8"
          git config user.email "0x3ef8@gmail.com"
          count=0
          while [ $count -lt 100 ]; do
            # Dynamically change the content to ensure a new commit
            echo "  ________.__  __     ___ ___         .__  .__   " > abyss/README.yml
            echo " /  _____/|__|/  |_  /   |   \   ____ |  | |  |  " >> abyss/README.yml
            echo "/   \  ___|  \   __\/    ~    \_/ __ \|  | |  |  " >> abyss/README.yml
            echo "\    \_\  \  ||  |  \    Y    /\  ___/|  |_|  |__" >> abyss/README.yml
            echo " \______  /__||__|   \___|_  /  \___  >____/____/ " >> abyss/README.yml
            echo "        \/                 \/       \/            " >> abyss/README.yml
            echo "----------------------------------------------------" >> abyss/README.yml
            echo "If you think you’ve won, think again. HAHAHAHA!" >> abyss/README.yml
            echo "----------------------------------------------------" >> abyss/README.yml
            commit_id=$(cat /proc/sys/kernel/random/uuid)
            timestamp=$(date --utc +%Y-%m-%dT%H:%M:%SZ)
            total_commits=$(git rev-list --count HEAD)
            echo "Commit ID: $commit_id" >> abyss/README.yml
            echo "Timestamp: $timestamp" >> abyss/README.yml
            echo "Commit: $total_commits" >> abyss/README.yml
            echo "----------------------------------------------------" >> abyss/README.yml
            git add abyss/README.yml
            git commit -S -m "If your strategy’s dirty, mine’s worse. 😈 (Total commits: $total_commits)"
            count=$((count+1))
          done
          git push
        env:
          API_GITHUB_TOKEN: ${{ secrets.API_GITHUB_TOKEN }}
